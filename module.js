// Some useful modules in nodejs

// http server 
var http = require('http');

// file reading
var fs = require('fs');

// Event listner 
var events = require('events')
var eventEmitter = new events.EventEmitter()

// Working with buffer to handle binary data.
// JS are unicode friendly not so binary friendly
// Node has buffer class to handle the raw of streams


